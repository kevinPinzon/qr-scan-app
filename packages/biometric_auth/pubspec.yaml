name: biometric_auth
description: "Paquete para manejar la autenticación biométrica en la app QR."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  pigeon: 21.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  plugin:
    platforms:
      android:
        package: dev.flutter.biometric_auth
        pluginClass: BiometricAuth